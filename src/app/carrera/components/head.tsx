import logo from '@/../public/logo.svg'
import Image from 'next/image'
export const HeadBar = () => {
  return (
    <nav className='w-screen h-[65px] bg-[--main-yellow] top-0 left-0 absolute'></nav>
  )
}
